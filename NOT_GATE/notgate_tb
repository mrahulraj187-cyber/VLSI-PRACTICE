module notgate_tb();

reg a;
wire y;

notgate dut(
    .a(a),
    .y(y)
);

initial begin
    $dumpfile("notgate.vcd");
    $dumpvars(0, notgate_tb);

    $monitor("a=%b, y=%b", a, y);

    a = 0; 
    #5;
    a = 1; 
    #5;

    $finish;
end

endmodule
