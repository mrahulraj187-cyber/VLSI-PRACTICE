module parity_generator_tb();
reg [3:0]a;
wire parity;

parity_generator dut(
    .a(a),
    .parity(parity)
);

integer k;

initial begin
    $dumpfile("parity_generator.vcd");
    $dumpvars(0,parity_generator_tb);
    $monitor("a=%b,parity=%b",a,parity);
    
    for(k=0;k<16;k=k+1)begin
        a=k;
        #5;
    end
    $finish;
end
endmodule
