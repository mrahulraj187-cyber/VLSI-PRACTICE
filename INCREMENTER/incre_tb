module incrementer_tb();
reg [3:0]a;
wire[3:0]b;

incrementer dut(
    .a(a),
    .b(b)
);

integer k;

initial begin
    $dumpfile("incrementer.vcd");
    $dumpvars(0,incrementer_tb);
    $monitor("a=%b,b=%b",a,b);

    for(k=0 ; k<8 ;k=k+1)begin
        a = k;
        #5;

    end
    $finish;
end
endmodule 
