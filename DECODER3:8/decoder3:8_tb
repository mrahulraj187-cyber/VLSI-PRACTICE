module decoder3to8_tb();

reg [2:0] a;
wire [7:0] y;

decoder3to8 dut(
    .a(a),
    .y(y)
);

integer i;

initial begin

    $dumpfile("decoder3to8.vcd");
    $dumpvars(0, decoder3to8_tb);

    $monitor("a=%b, y=%b", a, y);

    for(i=0; i<8; i=i+1) begin
        a=i;
        #5;
    end

    $finish;

end

endmodule
