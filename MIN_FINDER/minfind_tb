module min_finder_tb();

reg [3:0] a;
reg [3:0] b;
wire [3:0] min;

min_finder dut(
    .a(a),
    .b(b),
    .min(min)
);

integer k;

initial begin
    $dumpfile("min_finder.vcd");
    $dumpvars(0, min_finder_tb);

    $monitor("a=%d, b=%d, min=%d", a, b, min);

    for(k=0; k<10; k=k+1) begin
        a = k;
        b = 9-k;
        #5;
    end

    $finish;
end

endmodule
