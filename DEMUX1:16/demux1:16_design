module demux16(
    input i,
    input [3:0] s,
    output reg [15:0] y
);

integer k;

always @(*) begin

    y = 16'b0;

    for(k=0; k<16; k=k+1) begin
        if(s == k)
            y[k] = i;
    end

end

endmodule
