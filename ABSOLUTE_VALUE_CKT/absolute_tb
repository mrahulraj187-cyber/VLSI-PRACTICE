module absolute_value_tb();

reg signed [3:0] a;
wire signed [3:0] y;

absolute_value dut(
    .a(a),
    .y(y)
);

integer k;

initial begin
    $dumpfile("absolute_value.vcd");
    $dumpvars(0, absolute_value_tb);

    $monitor("a=%d, y=%d", a, y);

    for(k=-8; k<8; k=k+1) begin
        a = k;
        #5;
    end

    $finish;
end

endmodule
