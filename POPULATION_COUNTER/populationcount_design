module population_counter(
    input [3:0] a,
    output reg [2:0] count
);

integer i;

always @(*) begin
    count = 0;

    for(i=0; i<4; i=i+1) begin
        count = count + a[i];
    end
end

endmodule
