module andgate_tb();
   reg a,b;
   wire y;
andgate dut(
    .a(a),
    .b(b),
    .y(y)
);
initial begin
    $dumpfile("andgate.vcd");
    $dumpvars(0,andgate_tb);
    $monitor("a=%b,b=%b,y=%b",a,b,y);
    a=0;b=0;
    #5;
    a=0;b=1;    
    #5;
    a=1;b=0;
    #5;
    a=1;b=1;
    #5;
    $finish;
end

endmodule
