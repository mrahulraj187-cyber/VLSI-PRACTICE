module decoder2to4_tb();

reg [1:0] a;
wire [3:0] y;

decoder2to4 dut(
    .a(a),
    .y(y)
);

integer i;

initial begin

    $dumpfile("decoder2to4.vcd");
    $dumpvars(0, decoder2to4_tb);

    $monitor("a=%b, y=%b", a, y);

    for(i=0; i<4; i=i+1) begin
        a=i;
        #5;
    end

    $finish;
end

endmodule
