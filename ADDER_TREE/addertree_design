module adder_tree(
    input [3:0] a,
    input [3:0] b,
    input [3:0] c,
    input [3:0] d,
    output [5:0] sum
);

wire [4:0] x;
wire [4:0] y;

assign x = a + b;
assign y = c + d;

assign sum = x + y;

endmodule
